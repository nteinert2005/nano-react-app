import React from 'react';
import styled from 'styled-components';

const Home = () => {
    return (
        <div>
        </div>
    );
};

const HomeTest = styled.div`
    color: red;
`

export default Home;